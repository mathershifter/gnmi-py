gnmi_version: 0.9.0
target: localhost:50051
metadata:
    username: admin
    password: ""

get:
    prefix: /interfaces
    paths:
        - /interface[name=Ethernet3/1]/state/counters
    type: all
    encoding: json